[phases.setup]
nixPkgs = ["python3", "nodejs_20"]

[phases.install]
cmds = [
    "python -m venv /app/.venv",
    "/app/.venv/bin/pip install -r requirements.txt",
    "cd frontend && rm -rf package-lock.json node_modules && npm install --legacy-peer-deps"
]

[phases.build]
cmds = [
    "cd frontend && npm run build"
]

[start]
cmd = "/app/.venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"
